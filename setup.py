from setuptools import setup, find_packages

setup(
    name='csvinspector',
    version='1.0.0',
    description='CSV Validation and Analysis Tool',
    author='Lukas Velička',
    packages=find_packages(),
    install_requires=[],
    python_requires='>=3.7',
)
